custom: -t @custom_conversions
home: -t @home_screen
main: -t @main_menu

# Create HTML reports in profiles
# --format html --out report.html --format pretty

# Run tests that have any of the tags
any: -t @custom_conversions, @home_screen
# Run tests that have both tags
strict: -t @custom_conversions -t @home_screen